import { defineConfig } from 'astro/config';

// Deployed via GitHub Pages initially at rahmipruitt.me (custom domain).
// site/base assume a custom domain (repo root), not a github.io project path.
export default defineConfig({
  site: 'https://rahmipruitt.me',
});
